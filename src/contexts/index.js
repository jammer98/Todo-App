export { TodoContext,TodoProvider,useTodo } from "./TodoContext";

// we craeted a index.js file and we got all the exports in here because by this we can export everything from here 
// without exporting every file
